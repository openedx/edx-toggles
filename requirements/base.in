# Core requirements for using this application

-c constraints.txt

code-annotations           # Parse annotations in codebases
Django>=2.2                # Web application framework
django-crum                # Middleware that stores the current request and user in thread local storage
django-waffle              # Django feature flipper
edx-django-utils           # Utilities for cache and monitoring
