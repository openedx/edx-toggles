# Requirements for running the utility scripts found in the 'scripts' directory

-r base.txt               # Core dependencies for this package

atlassian-python-api      # provides an interface for creating Confluence pages
code-annotations          # provides commands for identifying annotations and generating reports
mysql-connector-python-rf # provides an interface for querying a MySQL server
